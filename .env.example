# Backend API base URL (no trailing slash).
# For local dev you can leave unset (defaults to http://localhost:8000).
# For Vercel/production you MUST set this to your deployed backend URL, e.g.:
# NEXT_PUBLIC_API_URL=https://your-backend.up.railway.app
# NEXT_PUBLIC_API_URL=
